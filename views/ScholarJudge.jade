html
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', ,='', content='IE=edge')
    meta(name='viewport', ,='', content='width=device-width, initial-scale=1')
    link(rel='stylesheet', href='/vendor/bootstrap/dist/css/bootstrap.min.css')
    link(rel='stylesheet', href='/stylesheets/Adminjudge.css')
    link(rel='stylesheet', href='/stylesheets/default.css')
    title 奖学金评比
  body
    nav.navbar.navbar-inverse
      .container
        .navbar-header
          button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#nav-collapse', aria-expanded='false')
            span.icon-bar
            span.icon-bar
            span.icon-bar
          a.navbar-brand(href='#')
            if user
              | #{user.student_id}奖学金评比信息
        #nav-collapse.collapse.navbar-collapse
          ul.nav.navbar-nav
            li
              if user
                if user.file_id
                  a(href='/scholar-getfile?fileid=#{user.file_id}') 
                    if comment
                      下载该生资料
                    else
                      下载最近提交资料
          if user
           p.navbar-text.navbar-right
              | 当前分数为 ：#{user.point}
    .container
      .row
        .his-msg
          if user
            h1 历史评论
            p.his-msg-content
              if user.comment
                p
                  !=user.comment
              else
                | 暂无
            if user.comment
              p.
                最后由 #{user.username} 评论
          else
            h1 暂未提交资料
      if comment
        .row
          .current-msg
            textarea#comment.form-control(rows='6', placeholder='请输入评价依据')
          if comment
            .form-group
              input.form-control#point(type='text', placeholder='分数')
            button.btn.btn-danger#point-btn(type='button') 提交
    script(src='/vendor/jquery/dist/jquery.min.js')
    script(src='/vendor/bootstrap/dist/js/bootstrap.min.js')
    script(Src='/javascripts/scholar.js')