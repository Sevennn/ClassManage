var mysql = require('mysql');
var conn = require('./base');


module.exports = {
    CreateFile: function(filename, filepath, callback) {
        var sql = 'insert into file values(';
        conn.query()
    }
}